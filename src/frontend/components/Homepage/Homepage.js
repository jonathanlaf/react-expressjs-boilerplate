import React from 'react';

const Homepage = ({boilerplateVersion}) => (
		<section>
				<article>
						<h2>Home page - Boilerplate v{boilerplateVersion}</h2>
						<hr/>
						<p>Lorem lipsum...</p>
				</article>
		</section>
);

export default Homepage;